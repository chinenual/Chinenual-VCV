#pragma once

#define NUM_TRACKS 10

// the Chinenual logo color:
static const NVGcolor logoTextColor = nvgRGB(0xff, 0xd5, 0x56);
// white:
static const NVGcolor whiteTextColor = nvgRGB(0xff, 0xff, 0xff);
// red:
// static const NVGcolor ledTextColor = nvgRGB(0xff, 0x00, 0x00);
// lighter red:
static const NVGcolor ledTextColor = nvgRGB(0xff, 0x33, 0x33);
